# AI Usage

## Tools
* Cursor (model: `auto`)
  * I interacted with this tool through the Cursor IDE, a clone of VS code with integrated agentic support.

## What I Wrote/Decided
* Tech Stack - No DB, C# backend, Vite + React frontend
  * dotnet webapi template - Considered using a serverless function and hosting with AWS Lambda. Would probably use that in production for this simple of a function that runs on demand like this. But in the interest of learning more about the framework and to demonstrate some project structure considerations I decided to create a lightweight API and host with AWS Elastic Beanstalk. 
  * Vite + React frontend - To handle most of the validation with reactive feedback before calling the API.
* API design
  * architecture 
  * contract (req/resp models)
* API implementation - because I am familiarizing with C#, I was more hands on with the implementation of this project than I may usually be with Java or Python. Here are the tasks I manually performed:
  * removed unnecessary boilerplate generated by the `dotnet new webapi` template (removed the OpenAPI Service to keep code clean, but would definitely use that in production for API documentation).
  * created `/Controllers` directory and `NumberWordsController.cs`, following [mvc documentation](https://learn.microsoft.com/en-us/aspnet/core/mvc/overview?view=aspnetcore-10.0) 
  * created `/Models` directory and subfolders
    * `/DomainModel` - contains business logic. currently contains a `/Service` directory, but could hold constants, enums, etc that are specific to the business domain
    * `/RequestDtos` - models of Requests bodies for clear API contract. Using DataAnnotation to validate on Dtos following [official documentation](https://learn.microsoft.com/en-us/dotnet/api/system.componentmodel.dataannotations?view=net-10.0)
    * `/ResponseDtos` - models of Response bodies for clear API contract.
* Frontend Implementation
  * created frontend scaffolding in monorepo using vite: `npm create vite@latest frontend -- --template react-ts`, removed example code
  



## Edge Case: Int64.MinValue Overflow (AI-Assisted Discovery)

While testing boundary values, I sent `−9223372036854775808` (`Int64.MinValue`) through the app and the server crashed. Working with AI, we identified a two-layer bug:

1. **Backend**: `Math.Abs(long.MinValue)` overflows because its absolute value exceeds `long.MaxValue`. Fixed by converting to `ulong` before processing.
2. **Frontend serialization**: `values.map(Number)` silently lost precision for 64-bit integers (JS doubles only have 53 bits of integer precision). Fixed by building the JSON body as a raw string so numeric literals hit the wire without JavaScript float conversion.

